{
  "name": "testproj",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "lighting02build": "vite build lighting02",
    "lighting02preview": "vite preview lighting02 --host",
    "villagebuild": "tsc && vite build village",
    "villagepreview": "vite preview village --host",
    "collision03build": "vite build collision03",
    "collision03preview": "vite preview collision03 --host",
    "Guibuild": "vite build Gui",
    "Guipreview": "vite preview Gui --host"

  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  }
}
